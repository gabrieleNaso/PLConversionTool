# Data

Cartella unica per input, output, tmp e dataset del progetto.

Struttura:
- `input/`  sorgenti AWL da convertire
- `output/` artefatti generati dal tool
- `tmp/`    staging e file temporanei
- `datasets/` campioni di riferimento (corpus, typicals, golden)
