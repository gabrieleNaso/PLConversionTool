Metti qui i sorgenti AWL da convertire in batch.

Formati supportati:

- `.awl`
- `.txt`
- `.md`

Poi esegui dalla root del progetto:

```bash
make generate-input
```

Gli output vengono creati in `output/generated/`.
